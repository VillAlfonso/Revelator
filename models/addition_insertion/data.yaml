# ForgeGuard - Addition: Insertion
# YOLO Training Configuration
# Category: Alteration
# 
# This is a BINARY CLASSIFIER:
#   Class 0: addition_insertion_detected (forgery of this type found)
#
# Text or characters inserted into existing spaces within a document.

# Dataset paths (relative to this file)
path: ./dataset
train: images/train
val: images/val

# Single class - binary detection
nc: 1
names:
  0: addition_insertion_detected

# Training notes:
# - Minimum 100 images recommended
# - 80/20 train/val split
# - Label format: YOLO (class x_center y_center width height)
# - All coordinates normalized 0-1
