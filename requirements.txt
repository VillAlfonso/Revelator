# ForgeGuard Backend Requirements
# Tested with Python 3.11

# Core API
fastapi==0.109.2
uvicorn[standard]==0.27.1
python-multipart==0.0.9
python-dotenv==1.0.1
pydantic==2.6.1

# Image Processing
Pillow==10.2.0

# AI/ML
ultralytics==8.1.0
# For scripts/fetch_sources.py — streams public document datasets from HF Hub.
# Optional: only required if you use the dataset-fetcher script.
datasets>=2.18.0
# For scripts/fetch_roboflow.py — pulls your Roboflow-labeled data locally.
# Optional: only required if you have hand-crafted forgeries on Roboflow.
roboflow>=1.1.0

# LLM Options
groq==0.4.2
requests==2.31.0

# Development
pytest==8.0.0
httpx==0.26.0
